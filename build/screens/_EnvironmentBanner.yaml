# Environment Banner — shared app chrome
# A single centered label across the top of every screen, showing which
# environment the app is running in. Classic controls only (Constitution II).
#
# ---------------------------------------------------------------------------
# THIS IS A SHARED PASTE BLOCK
# ---------------------------------------------------------------------------
# Paste it on every screen. Studio will auto-suffix duplicate names
# (lblEnvBanner_1, _2 …) on the second and later screens — that is FINE;
# nothing references this control by name.
#
# Change the environment name in ONE place: the Text below. All screens'
# banners update the next time each is re-pasted. (Converting the shared
# blocks to canvas components is the T083 refactor.)
#
# ---------------------------------------------------------------------------
# PLACEMENT ON EACH SCREEN
# ---------------------------------------------------------------------------
# Sits at the very top (Y = 0), full width, 24px tall. Paste this BEFORE the
# utility bar — the utility bar's Y is `If(lblEnvBanner.Height > 0, ..., 0)`
# and needs `lblEnvBanner` to already exist on the screen when it resolves.
#
# The utility bar shifts down automatically to sit below this banner. Screens
# that use the utility bar MUST also paste this file, or the utility bar's Y
# reference to `lblEnvBanner.Height` won't resolve.
#
# To hide the banner (e.g. for PROD), set Height = 0 below and the utility
# bar collapses back to Y = 0 with no other edits. Screen content still needs
# to be shifted independently — that reflow is a separate change (T083).

- lblEnvBanner:
    Control: Label@2.5.1
    Properties:
      Align: =Align.Center
      Color: =ColorPalette.TextOnDark
      Fill: =RGBA(95, 127, 35, 1)
      Font: =Typography.Family
      FontWeight: =Typography.WeightBold
      Height: =24
      Size: =Typography.SizeCaption
      Text: ="DEV"
      Width: =Parent.Width
      X: =0
      Y: =0
